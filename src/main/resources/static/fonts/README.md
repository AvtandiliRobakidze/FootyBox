# Local Fonts

Place only licensed self-hosted font files here. Record the family, source, license, and required attribution in `../assets/asset-manifest.json`.

The CSS currently falls back to system fonts until approved Archivo and Inter files are supplied.
